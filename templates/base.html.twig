<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}BMW Motorrad{% endblock %}</title>

    {# Solución al bloqueo: Favicon mediante Data URI para evitar peticiones externas bloqueantes #}
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🏍️</text></svg>">

    {# Meta tags para asegurar que el navegador no se quede con versiones bloqueadas de la página #}
    <meta name="turbo-visit-control" content="reload">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    {% block stylesheets %}{% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% endblock %}

    <style>
        :root {
            --bmw-blue: #0066b1;
            --bmw-dark: #262626;
        }
        body { background-color: #f4f7f6; }
        .navbar-bmw { background-color: #ffffff; border-bottom: 3px solid var(--bmw-blue); }
        /* Evita que los enlaces parezcan morados */
        a { text-decoration: none !important; }
        .nav-link-custom { font-weight: bold; color: var(--bmw-dark) !important; transition: 0.3s; }
        .nav-link-custom:hover { color: var(--bmw-blue) !important; }
    </style>
</head>
{# data-turbo="false" garantiza que el teclado y los formularios respondan de forma tradicional #}
<body data-turbo="false">
<nav class="navbar navbar-expand-lg navbar-bmw shadow-sm mb-5 py-3">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('app_moto') }}" style="color: var(--bmw-blue);">
            <i class="fas fa-motorcycle me-2"></i>MOTOS BMW
        </a>

        <div class="d-flex align-items-center">
            {% if app.user %}
                <span class="me-3 text-muted d-none d-md-inline small">
                    Bienvenido, <strong>{{ app.user.nombre|default('Usuario') }}</strong>
                </span>

                {% if is_granted('ROLE_ADMIN') %}
                    <a class="btn btn-warning btn-sm fw-bold px-3 shadow-sm me-2 text-dark" href="/admin">
                        <i class="fas fa-user-shield me-1"></i> PANEL ADMIN
                    </a>
                {% else %}
                    <a class="btn btn-primary btn-sm fw-bold px-3 shadow-sm me-2" href="{{ path('app_user_dashboard') }}" style="background-color: var(--bmw-blue); border: none;">
                        <i class="fas fa-user-circle me-1"></i> MI CUENTA
                    </a>
                {% endif %}

                <a href="{{ path('app_logout') }}" class="btn btn-outline-danger btn-sm px-2 shadow-sm" title="Cerrar Sesión">
                    <i class="fas fa-power-off"></i>
                </a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-outline-dark btn-sm fw-bold me-2 px-3">
                    <i class="fas fa-sign-in-alt me-1"></i> Entrar
                </a>
                <a href="{{ path('app_register') }}" class="btn btn-primary btn-sm px-3 rounded-pill fw-bold shadow-sm" style="background-color: var(--bmw-blue); border: none;">
                    Registrarse
                </a>
            {% endif %}
        </div>
    </div>
</nav>

<main class="container">
    {% block body %}{% endblock %}
</main>

<footer class="text-center py-4 mt-5 border-top text-muted small">
    &copy; {{ "now"|date("Y") }} MOTOS BMW
</footer>
</body>
</html>
